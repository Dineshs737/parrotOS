#include"./lib.h">

void insertAtBeginning(medicine mediData){
  /*  medicine* newMedicine =(medicine*)malloc(sizeof(medicine));
    if(newMedicine==NULL) printf("out of memory"); return;

    strcapy(newMedicine->name,mediData.name);
    strcapy(newMedicine->use,mediData.use);
    newMedicine->date = mediData.date;
    newMedicine->stock =mediData.stock;


    if(head==NULL){
        newMedicine->next =NULL;
        head =newMedicine;
    }
    else{
        newMedicine ->next =head;
        head =newMedicine;


    }*/
    printf("inserted  At Beginning \n");

}